# include <iostream>
using namespace std;

int main()
{
   int a[10];
   int i;
   int count1=0;
   int count2;
   cout<<"enter numbers";
   for(i=0;i<10;i++)
   {
      cin>>a[i] ;

   }
 if(a[i]<0)
        {
           count1+=count1;
           cout<<count1;
        }
        else if(a[i]>0)
        {
            count2=+count2;
            cout<<count2;
        }

    return 0;
}D